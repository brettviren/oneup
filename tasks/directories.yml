---

- file: state=directory path=$build_path
  when_set: $build_path
  tags:
    - directories

# - file: state=directory path=$source_path
#   when_set: $source_path
#   tags:
#     - directories

- file: state=directory path=$install_path
  when_set: $install_path
  tags:
    - directories

  